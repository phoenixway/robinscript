package network

import (
	"testing"
)

func TestHub(t *testing.T) {
	var h = Hub{}
	h.Init()
	/*
		connect new user
		check if it added as guest
		login
		check
		send signal
		check
		disconnect
		check
	*/
}
